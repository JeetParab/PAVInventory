<Window x:Class="PAV.Client.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BgBrush}">
  <DockPanel Margin="24">
    <TextBlock DockPanel.Dock="Top" Text="{Binding Title}" Style="{StaticResource PageTitle}" Margin="0,0,0,8"/>
    <TextBlock DockPanel.Dock="Top" Text="{Binding Intro}" Style="{StaticResource MutedText}"
               TextWrapping="Wrap" Margin="0,0,0,14"/>
    <TextBlock DockPanel.Dock="Top" Text="{Binding Error}" Foreground="{DynamicResource DangerBrush}"
               TextWrapping="Wrap" Margin="0,0,0,8"/>
    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}"
              Margin="0,0,8,0" IsCancel="True"/>
      <Button Content="Save password" Style="{StaticResource PrimaryButton}" Click="OnSave" IsDefault="True"/>
    </StackPanel>
    <StackPanel>
      <TextBlock Text="Current password" Style="{StaticResource FieldLabel}"/>
      <PasswordBox x:Name="CurrentBox" Margin="0,0,0,10" Padding="8,6"/>
      <TextBlock Text="New password" Style="{StaticResource FieldLabel}"/>
      <PasswordBox x:Name="NewBox" Margin="0,0,0,10" Padding="8,6"/>
      <TextBlock Text="Confirm new password" Style="{StaticResource FieldLabel}"/>
      <PasswordBox x:Name="ConfirmBox" Padding="8,6"/>
    </StackPanel>
  </DockPanel>
</Window>
