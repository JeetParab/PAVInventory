<Window x:Class="PAV.Client.Views.StockMoveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize" Background="{DynamicResource BgBrush}">
  <DockPanel Margin="22">
    <TextBlock DockPanel.Dock="Top" Text="{Binding Title}" Style="{StaticResource PageTitle}" Margin="0,0,0,6"/>
    <TextBlock DockPanel.Dock="Top" Text="{Binding Hint}" Style="{StaticResource MutedText}" Margin="0,0,0,12"/>
    <TextBlock DockPanel.Dock="Top" Text="{Binding Error}" Foreground="{DynamicResource DangerBrush}"
               TextWrapping="Wrap" Margin="0,0,0,8"/>
    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}"
              Margin="0,0,8,0" IsCancel="True"/>
      <Button Content="{Binding ActionLabel}" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" IsDefault="True"/>
    </StackPanel>
    <StackPanel>
      <TextBlock Text="Item" Style="{StaticResource FieldLabel}"/>
      <ComboBox ItemsSource="{Binding Items}" SelectedItem="{Binding Item}" DisplayMemberPath="Name" Margin="0,0,0,10"/>
      <TextBlock Text="Quantity" Style="{StaticResource FieldLabel}"/>
      <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
      <StackPanel Visibility="{Binding NeedsUser, Converter={StaticResource BoolToVis}}">
        <TextBlock Text="User" Style="{StaticResource FieldLabel}"/>
        <ComboBox ItemsSource="{Binding Users}" Text="{Binding AssignedUserName, UpdateSourceTrigger=PropertyChanged}"
                  IsEditable="True" IsTextSearchEnabled="False" DisplayMemberPath="AssignLabel"
                  MaxDropDownHeight="360" StaysOpenOnEdit="True" Margin="0,0,0,4"/>
        <TextBlock Text="{Binding UserHint}" Style="{StaticResource MutedText}" TextWrapping="Wrap" Margin="0,0,0,10"/>
      </StackPanel>
      <TextBlock Text="Reference" Style="{StaticResource FieldLabel}"/>
      <TextBox Text="{Binding Reference, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
      <TextBlock Text="Serial (if any)" Style="{StaticResource FieldLabel}"/>
      <TextBox Text="{Binding SerialNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
      <TextBlock Text="Notes / reason" Style="{StaticResource FieldLabel}"/>
      <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Height="64"
               TextWrapping="Wrap" AcceptsReturn="True"/>
    </StackPanel>
  </DockPanel>
</Window>
