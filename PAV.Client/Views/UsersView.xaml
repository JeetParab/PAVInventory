<UserControl x:Class="PAV.Client.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Margin="0,0,0,10">
      <TextBlock Text="Users" Style="{StaticResource PageTitle}"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Margin="0,0,8,0" Padding="12,8">
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="&#xE72C;" Style="{StaticResource BtnIcon}"
                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
            <TextBlock Text="Refresh" Style="{StaticResource BtnLabel}"/>
          </StackPanel>
        </Button>
        <Button Content="+ Add user" Style="{StaticResource PrimaryButton}" Command="{Binding AddCommand}"
                IsEnabled="{Binding CanManage}" Margin="0,0,8,0"/>
        <Button Content="Edit" Style="{StaticResource SecondaryButton}" Command="{Binding EditCommand}"
                IsEnabled="{Binding CanManage}"/>
      </StackPanel>
    </DockPanel>

    <StackPanel Grid.Row="1" Margin="0,0,0,10">
      <TextBlock Style="{StaticResource MutedText}" TextWrapping="Wrap" Margin="0,0,0,8"
                 Text="PAV users are the same people used in Inventory assign and Stock issue. AssignedUserId is the link — not the display name."/>
      <TextBlock Style="{StaticResource MutedText}" TextWrapping="Wrap" Foreground="#C47B17"
                 Text="{Binding UnlinkedSummary}"
                 Visibility="{Binding HasUnlinked, Converter={StaticResource BoolToVis}}"/>
    </StackPanel>

    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="420" MinWidth="280"/>
        <ColumnDefinition Width="12"/>
        <ColumnDefinition Width="*" MinWidth="360"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
        <DockPanel>
          <TextBox DockPanel.Dock="Top" Margin="10,10,10,8"
                   Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                   VerticalContentAlignment="Center"
                   ToolTip="Search name, username, department, location"/>
          <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding Selected}"
                    EnableRowVirtualization="True" EnableColumnVirtualization="False"
                    CanUserResizeColumns="True"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">
            <DataGrid.InputBindings>
              <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditCommand}"/>
            </DataGrid.InputBindings>
            <DataGrid.Columns>
              <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="140" MinWidth="100"/>
              <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="100"/>
              <DataGridTextColumn Header="Dept" Binding="{Binding Department}" Width="90"/>
              <DataGridTextColumn Header="Assets" Binding="{Binding AssetCount}" Width="55"/>
              <DataGridTextColumn Header="Stock" Binding="{Binding StockWithUser}" Width="55"/>
              <DataGridTextColumn Header="Role" Binding="{Binding RoleName}" Width="90"/>
            </DataGrid.Columns>
          </DataGrid>
        </DockPanel>
      </Border>

      <Grid Grid.Column="2">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" MinHeight="160"/>
          <RowDefinition Height="12"/>
          <RowDefinition Height="*" MinHeight="140"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource Card}" Padding="0">
          <DockPanel>
            <TextBlock DockPanel.Dock="Top" Margin="12,8,12,6" FontWeight="SemiBold"
                       Text="{Binding HoldingsTitle}" TextWrapping="Wrap"/>
            <DataGrid ItemsSource="{Binding Assets}" SelectedItem="{Binding SelectedAsset}"
                      EnableRowVirtualization="True" EnableColumnVirtualization="False"
                      CanUserResizeColumns="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Asset ID" Binding="{Binding AssetTag}" Width="130"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                <DataGridTextColumn Header="Make / model" Binding="{Binding MakeModel}" Width="160"/>
                <DataGridTextColumn Header="Serial" Binding="{Binding SerialNumber}" Width="130"/>
                <DataGridTextColumn Header="Hostname" Binding="{Binding Hostname}" Width="130"/>
                <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="120"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
              </DataGrid.Columns>
            </DataGrid>
          </DockPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
          <DockPanel>
            <TextBlock DockPanel.Dock="Top" Margin="12,8,12,6" FontWeight="SemiBold"
                       Text="Stock issued / returned"/>
            <DataGrid ItemsSource="{Binding Stock}"
                      EnableRowVirtualization="True" EnableColumnVirtualization="False"
                      CanUserResizeColumns="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
              <DataGrid.Columns>
                <DataGridTextColumn Header="When" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/>
                <DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="180"/>
                <DataGridTextColumn Header="Type" Binding="{Binding MovementType}" Width="80"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding SignedQuantity}" Width="55"/>
                <DataGridTextColumn Header="Serial" Binding="{Binding SerialNumber}" Width="120"/>
                <DataGridTextColumn Header="By" Binding="{Binding CreatedBy}" Width="100"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="180"/>
              </DataGrid.Columns>
            </DataGrid>
          </DockPanel>
        </Border>
      </Grid>
    </Grid>
  </Grid>
</UserControl>
