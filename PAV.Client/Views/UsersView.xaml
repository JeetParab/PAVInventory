<UserControl x:Class="PAV.Client.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
      <TextBlock Text="Users" Style="{StaticResource PageTitle}"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="+ Add user" Style="{StaticResource PrimaryButton}" Command="{Binding AddCommand}"
                IsEnabled="{Binding CanManage}" Margin="0,0,8,0"/>
        <Button Content="Edit" Style="{StaticResource SecondaryButton}" Command="{Binding EditCommand}"
                IsEnabled="{Binding CanManage}"/>
      </StackPanel>
    </DockPanel>
    <TextBlock DockPanel.Dock="Top" Style="{StaticResource MutedText}" Margin="0,0,0,12"
               Text="PAV accounts are independent of Windows. Every change is logged with this username. Guest can only view and export."/>
    <Border Style="{StaticResource Card}" Padding="0">
      <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding Selected}">
        <DataGrid.InputBindings>
          <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditCommand}"/>
        </DataGrid.InputBindings>
        <DataGrid.Columns>
          <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="160"/>
          <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
          <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeId}" Width="110"/>
          <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="140"/>
          <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="130"/>
          <DataGridTextColumn Header="Role" Binding="{Binding RoleName}" Width="120"/>
          <DataGridTemplateColumn Header="Active" Width="80">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding IsActive}" VerticalAlignment="Center"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </DockPanel>
</UserControl>
