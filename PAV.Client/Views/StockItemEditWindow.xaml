<Window x:Class="PAV.Client.Views.StockItemEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize" Background="{DynamicResource BgBrush}">
  <DockPanel Margin="22">
    <TextBlock DockPanel.Dock="Top" Text="{Binding Title}" Style="{StaticResource PageTitle}" Margin="0,0,0,12"/>
    <TextBlock DockPanel.Dock="Top" Text="{Binding Error}" Foreground="{DynamicResource DangerBrush}"
               TextWrapping="Wrap" Margin="0,0,0,8"/>
    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}"
              Margin="0,0,8,0" IsCancel="True"/>
      <Button Content="Save" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" IsDefault="True"/>
    </StackPanel>
    <StackPanel>
      <TextBlock Text="Name" Style="{StaticResource FieldLabel}"/>
      <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
      <Grid Margin="0,0,0,10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="12"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock Text="Manufacturer" Style="{StaticResource FieldLabel}"/>
          <TextBox Text="{Binding Manufacturer, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <StackPanel Grid.Column="2">
          <TextBlock Text="Model" Style="{StaticResource FieldLabel}"/>
          <TextBox Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
      </Grid>
      <Grid Margin="0,0,0,10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="12"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
          <ComboBox ItemsSource="{Binding Categories}" Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}" IsEditable="True"/>
        </StackPanel>
        <StackPanel Grid.Column="2">
          <TextBlock Text="Unit" Style="{StaticResource FieldLabel}"/>
          <TextBox Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
      </Grid>
      <TextBlock Text="Minimum quantity (low-stock when on hand is at or below this)" Style="{StaticResource FieldLabel}"/>
      <TextBox Text="{Binding MinimumQuantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
      <CheckBox Content="Active" IsChecked="{Binding IsActive}" Margin="0,0,0,10"/>
      <TextBlock Text="Notes" Style="{StaticResource FieldLabel}"/>
      <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Height="64"
               TextWrapping="Wrap" AcceptsReturn="True"/>
    </StackPanel>
  </DockPanel>
</Window>
