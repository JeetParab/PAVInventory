<UserControl x:Class="PAV.Client.Views.PendingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Margin="0,0,0,10">
      <TextBlock Text="Pending" Style="{StaticResource PageTitle}"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Margin="0,0,8,0" Padding="12,8">
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="&#xE72C;" Style="{StaticResource BtnIcon}"
                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
            <TextBlock Text="Refresh" Style="{StaticResource BtnLabel}"/>
          </StackPanel>
        </Button>
        <Button Content="Confirm ME" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                Command="{Binding ConfirmCommand}"
                IsEnabled="{Binding CanConfirm}"
                ToolTip="Mark this ManageEngine PC as confirmed inventory"/>
        <Button Content="Complete details" Style="{StaticResource PrimaryButton}"
                Command="{Binding CompleteCommand}"
                IsEnabled="{Binding CanComplete}"/>
      </StackPanel>
    </DockPanel>

    <TextBlock Grid.Row="1" Style="{StaticResource MutedText}" TextWrapping="Wrap" Margin="0,0,0,10"
               Text="{Binding Summary}"/>

    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
      <DataGrid ItemsSource="{Binding Rows}" SelectedItem="{Binding Selected}"
                EnableRowVirtualization="True"
                CanUserResizeColumns="True"
                MouseDoubleClick="OnRowDoubleClick">
        <DataGrid.InputBindings>
          <MouseBinding Gesture="LeftDoubleClick" Command="{Binding OpenRowCommand}"/>
        </DataGrid.InputBindings>
        <DataGrid.Columns>
          <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="160"/>
          <DataGridTextColumn Header="IP" Binding="{Binding Address}" Width="130"/>
          <DataGridTextColumn Header="User" Binding="{Binding AssignedUser}" Width="160"/>
          <DataGridTextColumn Header="Asset" Binding="{Binding AssetTag}" Width="140"/>
          <DataGridTextColumn Header="Hostname" Binding="{Binding Hostname}" Width="160"/>
          <DataGridTextColumn Header="Missing" Binding="{Binding Missing}" Width="*"/>
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </Grid>
</UserControl>
